# Keep the pre-launch Cloudflare Pages build out of search results.
# Change this to true only when the custom production domain is ready to launch.
PUBLIC_SITE_INDEXABLE=false
#
# Keep private credentials unprefixed and access them only from server-side
# code or a separate backend. Static builds embed any value used at build time.
